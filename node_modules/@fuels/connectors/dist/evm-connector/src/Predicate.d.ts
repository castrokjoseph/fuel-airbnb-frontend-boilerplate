import type { EIP1193Provider } from './utils/eip-1193';
export declare class PredicateAccount {
    private predicate;
    getPredicateFromAddress(address: string, ethProvider: EIP1193Provider): Promise<{
        ethAccount: string;
        predicateAccount: string;
    } | undefined>;
    getPredicateAccounts(ethProvider: EIP1193Provider): Promise<Array<{
        ethAccount: string;
        predicateAccount: string;
    }>>;
}
