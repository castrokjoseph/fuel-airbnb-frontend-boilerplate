import type { FuelConnector } from 'fuels';
type DefaultConnectors = {
    devMode?: boolean;
};
export declare function defaultConnectors({ devMode, }?: DefaultConnectors): Array<FuelConnector>;
export {};
